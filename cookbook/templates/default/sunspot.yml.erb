<% @environments.each do |env| %>
<%= env %>:
  solr:
    hostname: <%= @hostnames[env] %>
    port: 8983
    log_level: <%= @log_levels[env] %>
    pid_dir: tmp/pids
    path: /solr/<%= env %>
<% end %>
