---
- name: 'build the Docker containers'
  command: |
    ./build.sh all
  args:
    chdir: '~/primero/docker'

- name: 'configure the Docker containers'
  command: |
    ./compose.configure.sh
  args:
    chdir: '~/primero/docker'

- name: 'start the Docker containers'
  command: |
    ./compose.prod.sh up -d
  args:
    chdir: '~/primero/docker'
