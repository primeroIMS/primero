import { setupMountedComponent } from "../../../../../../../test";

import FiltersExpansionPanel from "./component";

describe("<FormsList />/components/<FiltersExpansionPanel />", () => {
  let component;

  beforeEach(() => {
    ({ component } = setupMountedComponent(FiltersExpansionPanel));
  });

  it.skip("renders clear button", () => {});
  it.skip("clear button responds to onClick from pass function", () => {});
  it.skip("renders <FiltersExpansionPanel />", () => {});
});
