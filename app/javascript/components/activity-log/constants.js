export const COLUMN_NAMES = Object.freeze([
  "type",
  "record_type",
  "record_id",
  "display_id",
  "performed_by",
  "datetime",
  "record_access_denied",
  "data"
]);

export const TYPES = Object.freeze({
  transfer: "transfer"
});
