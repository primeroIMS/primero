<%= hidden_field_tag "child[posted_from]", "Browser" if @child.new_record? %>
<% # Record Owner form %>
<%= hidden_field_tag('child[record_state]', @child.record_state) %>
<%= hidden_field_tag('child[owned_by]', @child.owned_by) %>
<%= hidden_field_tag('child[created_by]', @child.created_by) %>
<%= hidden_field_tag('child[previously_owned_by]', @child.previously_owned_by) %>
<%= hidden_field_tag('child[module_id]', @child.module_id) %>
