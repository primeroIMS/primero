<div class="row">
  <div class="large-8 medium-4 columns">
  </div>
  <div class="large-4 medium-4 columns">
      <% if can? :create, Replication %>
        <%= link_to content_tag(:span, t("lookup.create"), class: 'create_user'), new_lookup_path,  class: 'btn green-button right' %>
      <% end %>
  </div>
</div>
<div class="row">
  <div class="large-12 medium-12 columns">
    <div class="page-content-new">
      <div class="side-tab-content full_width">
        <% if @lookups.present? %>
          <%= render :partial => "lookups/list_view_table", :locals => {:lookups => @lookups} %>
        <% end %>
        <div id="modal-dialog" class="hide"></div>
      </div>
    </div>
  </div>
</div>