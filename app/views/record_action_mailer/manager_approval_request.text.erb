===============================================

<%=
  t(
    'email_notification.approval_request',
    user: @approval_notification.user.user_name,
    approval_type: @approval_notification.approval_type,
    case_id: link_to(@approval_notification.child.short_id, url_for_v2(@approval_notification.child)),
    locale: @approval_notification.locale
  ).html_safe
%>
