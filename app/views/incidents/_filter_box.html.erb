<form class="filter_panel">
  <input type="hidden" name="order_by" value="<%= @order %>"/>
	<%= t("incidents.filter_by.label") %>:
  <span> <%= select_tag :filter, options_for_select([[ t("incidents.filer_by.all"), "all" ],[t("incidents.filer_by.active"),"active"],[t("incidents.filer_by.reunited"),"reunited"],
                                                     [t("incidents.filer_by.flagged"),"flag"]], @filter), { :onchange => "this.form.submit();"} %> </span>
</form>
