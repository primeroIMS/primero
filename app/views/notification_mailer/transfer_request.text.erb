===============================================

<%= t('email_notification.transfer_request',
      user: @transition.transitioned_by,
      agency: @transition&.transitioned_by&.agency&.name,
      record_type: t("forms.record_types.#{@transition&.record.class.parent_form}"),
      record_id: link_to(
          @transition&.record&.short_id, url_for_v2(@transition&.record)
      )
    ).html_safe %>
<%=t('email_notification.transfer_request_notes_html',
     request_transfer_notes: @transition.notes
   ).html_safe %>
