===============================================

<%= t('email_notification.approval_request', user: @user.user_name, approval_type: @approval_type, case_id: link_to(@child.case_id_display, @url + '/cases/' + @child.id)).html_safe %>
