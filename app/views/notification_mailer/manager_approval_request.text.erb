===============================================

<%=
  t(
    'email_notification.approval_request',
    user: @user.user_name, approval_type: @approval_type,
    case_id: link_to(@child.short_id, url_for_v2(@child))
  ).html_safe
%>
