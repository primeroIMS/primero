===============================================

<%= t('email_notification.approval_request', user: @user.user_name, approval_type: @approval_type, case_id: link_to(@child.short_id, @url + '/cases/' + @child.id)).html_safe %>
