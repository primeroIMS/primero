<div class="panel">
 <div class="panel_header">
  <h4>New Cases Assigned To You</h4>  
 </div>
 <div class="panel_content">
  <ul>
  	<% cases.each do |c| %>
    	<li><%= link_to "#{c.short_id} #{c.name} - #{field_format_date(c.created_at)}", case_path(c) %></li>
    <% end %>
  </ul>
 </div>
</div>