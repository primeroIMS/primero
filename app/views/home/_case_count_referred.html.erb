<%= display_stat({
  name: :referred,
  stat: :total,
  stats: stats,
  model: model,
  text: t('dashboard.total_referred'),
  filters: ["child_status=#{Record::STATUS_OPEN}", 'referred_users', 'record_state=true']
}) %>

<%= display_stat({
  name: :referred,
  stat: :new,
  stats: stats,
  model: model,
  text: t('dashboard.new_referred'),
  filters: ["child_status=#{Record::STATUS_OPEN}", 'new', 'referred_users', 'record_state=true']
}) %>

