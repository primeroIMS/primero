<%= display_stat_detailed({
  name: :risk_level,
  stat: :high_total,
  filters: [
    "child_status=#{Record::STATUS_OPEN}",
    "risk_level=#{Child::RISK_LEVEL_HIGH}",
    'record_state=true'
  ],
  additional_details: [
    {
      stat: :high,
      filters: [
        "child_status=#{Record::STATUS_OPEN}",
        "risk_level=#{Child::RISK_LEVEL_HIGH}",
        'new_other',
        'record_state=true'
      ],
      text: t('dashboard.new')
    }
  ],
  stats: stats,
  model: model,
  text: t('dashboard.high_risk'),
}) %>

<%= display_stat_detailed({
  name: :risk_level,
  stat: :low_total,
  filters: [
    "child_status=#{Record::STATUS_OPEN}",
    "risk_level=#{Child::RISK_LEVEL_LOW}",
    'record_state=true'
  ],
  additional_details: [
    {
      stat: :low,
      filters: [
        "child_status=#{Record::STATUS_OPEN}",
        "risk_level=#{Child::RISK_LEVEL_LOW}",
        'new_other',
        'record_state=true'
      ],
      text: t('dashboard.new')
    }
  ],
  stats: stats,
  model: model,
  text: t('dashboard.low_risk'),
}) %>
