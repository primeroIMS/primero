<%= display_stat_detailed({
  name: :cases_to_assign,
  stat: nil,
  filters: [],
  additional_details: [
    {
      stat: :high,
      filters: [
        "child_status=#{Record::STATUS_OPEN}",
        "risk_level=#{Child::RISK_LEVEL_HIGH}",
        'record_state=true',
        "owned_by=#{current_user.user_name}",
        "reassigned_tranferred_on=#{one_hour_overdue}"
      ],
      text: t('dashboard.high_risk')
    },
    {
      stat: :low,
      filters: [
        "child_status=#{Record::STATUS_OPEN}",
        "risk_level=#{Child::RISK_LEVEL_LOW}",
        'record_state=true',
        "owned_by=#{current_user.user_name}",
        "reassigned_tranferred_on=#{three_hours_overdue}"
      ],
      text: t('dashboard.low_risk')
    }
  ],
  stats: stats,
  model: model,
  text: t('dashboard.overdue_cases_to_assign'),
}) %>
