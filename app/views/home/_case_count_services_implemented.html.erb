<%= display_stat({
  name: :services_implemented,
  stat: :total,
  stats: stats,
  model: model,
  text: t('dashboard.all_services_implemented'),
  filters: [
    "child_status=#{Record::STATUS_OPEN}",
    'record_state=true',
    "workflow=#{Child::WORKFLOW_SERVICE_IMPLEMENTED}"
  ]
}) %>
