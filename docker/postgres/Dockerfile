#ARG BUILD_REGISTRY
#ARG POSTGRES_VERSION=10.22
#FROM ${BUILD_REGISTRY}postgres:${POSTGRES_VERSION}-alpine
#ARG BUILD_REGISTRY
#FROM ${BUILD_REGISTRY}postgres:10.7-alpine
FROM bitnami/postgresql:10.22.0